describe("Make a test that you can run", () => {
    test("Test that the test runs", async () => {
        const someString = "This is a string";
        expect(someString).toBe("This is a string");
    }, 30000);
});