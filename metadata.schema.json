{
    "type": "object",
    "additionalProperties": {
      "type": "object",
      "properties": {
        "title": {
          "type": "string"
        },
        "description": {
          "type": "string"
        },
        "viewType": {
          "type": "string",
          "enum": ["functionSnippetWithNumberedGuide", "functionSnippet", "triggerMatch", "triggerProject"]
        },
        "guide": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "title": {
                "type": "string"
              },
              "body": {
                "type": "string"
              }
            },
            "required": ["title"],
            "additionalProperties": false
          }
        }
      },
      "required": ["title", "description", "viewType"],
      "if": {
        "properties": {
          "viewType": { "const": "functionSnippetWithNumberedGuide" }
        }
      },
      "then": {
        "required": ["guide"]
      },
      "else": {
        "not": {
          "required": ["guide"]
        }
      }
    }
  }